[gd_scene load_steps=4 format=3]

; A cloud bank. One sphere carrying the cloud material, flattened by the script
; so it reads as a bank lying on the combat plane rather than a ball hanging
; over it. It has no Field child: a nebula's field IS its volume, so there is
; nothing else to draw.

[ext_resource type="Script" path="res://src/ui/terrain_feature.gd" id="1"]
[ext_resource type="ArrayMesh" path="res://assets/meshes/sphere.obj" id="2"]
[ext_resource type="Material" path="res://assets/materials/mat_nebula.tres" id="3"]

[node name="Nebula" type="Node3D"]
script = ExtResource("1")

[node name="Body" type="MeshInstance3D" parent="."]
mesh = ExtResource("2")
material_override = ExtResource("3")
cast_shadow = 0
