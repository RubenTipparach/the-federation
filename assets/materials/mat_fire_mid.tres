[gd_resource type="ShaderMaterial" load_steps=2 format=3]

; The tongue: the narrow, tall part of a hull fire that licks up out of the
; plate. alert at the rim and gold_hi in its throat, from data/palette.json.
;
; Held tighter than the base (a higher hot_focus) so most of its length stays
; orange, which is what reads as a flame at a distance.

[ext_resource type="Shader" path="res://assets/shaders/fx_billboard.gdshader" id="1"]

[resource]
render_priority = 2
shader = ExtResource("1")
shader_parameter/tint = Color(0.8118, 0.4157, 0.3255, 0.55)
shader_parameter/hot = Color(0.851, 0.7216, 0.4, 1.0)
shader_parameter/hot_focus = 3.6
shader_parameter/spin = 0.0
shader_parameter/occlude = 0.0
shader_parameter/softness = 0.9
shader_parameter/billboard = true
