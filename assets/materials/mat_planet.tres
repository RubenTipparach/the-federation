[gd_resource type="StandardMaterial3D" format=3]

; A world. Lit, and the only large solid in the arena, so it carries the
; palette's blue_hi rather than the rock brown: at a glance the difference
; between "a rock, expensive" and "a world, fatal" has to be the colour, since
; at the tactical camera's distance both are just discs.

[resource]
albedo_color = Color(0.494, 0.663, 0.812, 1.0)
roughness = 0.85
metallic = 0.0
